
  if (typeof exports === 'object') {
    // CommonJS
    module.exports = arbitraryEmitter
  } else if (typeof define === 'function' && define.amd) {
    // AMD. Register as an anonymous module.
    define([], arbitraryEmitter)
  } else {
    // Browser globals
    root.arbitraryEmitter = arbitraryEmitter
  }
}(this))
